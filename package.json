{
  "name": "tapathletes-com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Static homepage for tapathletes.com. Vite is used for local dev preview only — production deploys plain static HTML/CSS via GitHub Pages.",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build:pdf": "node scripts/build-pdf.mjs",
    "build:jsonld": "node scripts/build-jsonld.mjs"
  },
  "devDependencies": {
    "puppeteer": "^24.43.0",
    "vite": "^5.4.10"
  }
}
